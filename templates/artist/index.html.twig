{% extends 'base.html.twig' %}

{% block title %}Artiste{% endblock %}

{% block body %}
<canvas class="d-none d-md-block s-bg-purple w-100" height="20"></canvas>
<div class="container-xxl">

    <div class='d-flex justify-content-between mt-3'>
        <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
            <ol class="breadcrumb  my-2">
                <li class="breadcrumb-item"><a href="{{ path ('home') }}">Acceuil</a></li>
                <li class="breadcrumb-item active" aria-current="page">Artiste</li>
            </ol>
        </nav>
        {% if is_granted('ROLE_ADMIN') %}
        <a class="my-auto btn btn-outline-dark rounded-pill disctext" href={{ path ('app_artist_new') }}>Ajouter un nouvel Artiste</a>
    {% endif %}
    </div>
    <div class="row row-cols-1 row-cols-sm-3 row-cols-md-4 row-cols-lg-5 row-cols-xl-6 g-2 mt-2">
        {% for artist in artists %}
            <div class="col">
                <div class="position-relative d-flex flex-sm-column border border-dark rounded p-0 h-100">
                    <div class="ratio ratio-1x1">
                        <img src="{{ asset ('images/artist/') }}{{ artist.picture }}" class="img-fluid" alt="{{ artist.name }}" title="{{ artist.name }}">
                    </div>
                    <div class="col-8 col-md-12 col-sm-12 p-3 d-flex flex-column flex-grow-1 justify-content-sm-between">
                        <div>
                            <p class="h5 m-0">{{ artist.name }}</p>
                        </div>
                    </div>
                    <a href="{{ path ('app_artist_show', {id: artist.id}) }}" class="stretched-link"></a>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
